# Flux Panel 环境变量配置
# 复制此文件为 .env 并修改配置值
# 用法：docker compose -f docker-compose-go.yml --env-file .env up -d

# 数据库
DB_NAME=gost
DB_USER=gost
DB_PASSWORD=your_secure_password

# JWT 密钥（请修改为随机字符串）
JWT_SECRET=your_jwt_secret_here

# 端口映射
BACKEND_PORT=6365
FRONTEND_PORT=80
